<p id="notice"><%= notice %></p>

<h1>Kintais</h1>
hello! <%= current_user.email %>
<%= link_to "ログアウト", destroy_user_session_path, method: :delete %>


<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Start</th>
      <th>End</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kintais.each do |kintai| %>
      <tr>
        <td><%= kintai.user_id %></td>
        <td><%= kintai.start %></td>
        <td><%= kintai.end %></td>
        <td><%= link_to 'Show', kintai %></td>
        <td><%= link_to '退勤する', edit_kintai_path(kintai) %></td>
        <td><%= link_to 'Destroy', kintai, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Kintai', new_kintai_path %>

<%= link_to '出勤する', start_kintais_path %>
<%= link_to '退勤する', end_kintais_path %>
